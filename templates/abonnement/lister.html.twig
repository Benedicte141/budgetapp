{% extends 'base.html.twig' %}

{% block title %}Liste des abonnements{% endblock %}

{% block body %}


        <h1>Lister les abonnements</h1>
        <div class="join">
            <div class="joinDivSearch">
              <div>
                <input class="input input-bordered join-item" placeholder="Search" />
              </div>
            </div>
            <div class="indicator">
              <button class="btn join-item">Search</button>
            </div>
        </div>
        <table>
            <thead>
                <tr>
                    <th>Organisme</th>
                    <th>Abonnement</th>
                    <th>N° Client</th>
                    <th>Date souscription</th>
                    <th>Montant</th>
                    <th>Périodicité</th>
                    <th>Objet</th>
                </tr>
            </thead>
            <tbody>
                {% for a in abonnements %}
                <tr>
                    <td>{{ a.organisme.nom }}</td>
                    <td>{{ a.TypeAbonnement.nom }}</td>
                    <td>{{ a.numeroClient }}</td>
                    <td>{{ a.dateSouscription|date('d/m/Y') }}</td>
                    <td>{{ a.montant }} €</td>
                    <td>{{ a.periodicite.libelle }}</td>
                    <td>{{ a.objet }}</td>
                </tr>
                {% else %}
                    <td>Aucun abonnement n'a été trouvé.</td>
                {% endfor %}
            </tbody>
        </table>
{% endblock %}